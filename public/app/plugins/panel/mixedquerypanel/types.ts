export interface MixedQueryOptions {
  title: string;
  virtualScrollItemHeight: number;
}

export const defaults: MixedQueryOptions = {
  title: 'Mixed Query Panel',
  virtualScrollItemHeight: 30,
};
